# Evaluación práctica Spring Boot

Versión del proyecto: **java25**.

La aplicación utiliza H2 y carga automáticamente `schema.sql` y `data.sql`.

Los requerimientos R6 a R10 se desarrollan únicamente sobre la tabla `supplies`.

Ejecute:

```bash
./gradlew bootRun
```
