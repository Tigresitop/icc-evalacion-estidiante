# Módulo supplies

En los requerimientos R6 a R10 debe desarrollar el módulo `supplies`.

El módulo utiliza exclusivamente la tabla `supplies` y no necesita consultar otros repositorios.

Estructura esperada:

```text
supplies/
├── controllers
├── dtos
├── entities
├── exceptions
├── mappers
├── repositories
└── services
```
